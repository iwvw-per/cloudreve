// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/cloudreve/Cloudreve/v4/ent/abusereport"
	"github.com/cloudreve/Cloudreve/v4/ent/predicate"
	"github.com/cloudreve/Cloudreve/v4/ent/share"
	"github.com/cloudreve/Cloudreve/v4/ent/user"
)

// AbuseReportQuery is the builder for querying AbuseReport entities.
type AbuseReportQuery struct {
	config
	ctx          *QueryContext
	order        []abusereport.OrderOption
	inters       []Interceptor
	predicates   []predicate.AbuseReport
	withReporter *UserQuery
	withReported *UserQuery
	withShare    *ShareQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AbuseReportQuery builder.
func (arq *AbuseReportQuery) Where(ps ...predicate.AbuseReport) *AbuseReportQuery {
	arq.predicates = append(arq.predicates, ps...)
	return arq
}

// Limit the number of records to be returned by this query.
func (arq *AbuseReportQuery) Limit(limit int) *AbuseReportQuery {
	arq.ctx.Limit = &limit
	return arq
}

// Offset to start from.
func (arq *AbuseReportQuery) Offset(offset int) *AbuseReportQuery {
	arq.ctx.Offset = &offset
	return arq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (arq *AbuseReportQuery) Unique(unique bool) *AbuseReportQuery {
	arq.ctx.Unique = &unique
	return arq
}

// Order specifies how the records should be ordered.
func (arq *AbuseReportQuery) Order(o ...abusereport.OrderOption) *AbuseReportQuery {
	arq.order = append(arq.order, o...)
	return arq
}

// QueryReporter chains the current query on the "reporter" edge.
func (arq *AbuseReportQuery) QueryReporter() *UserQuery {
	query := (&UserClient{config: arq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := arq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := arq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(abusereport.Table, abusereport.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, abusereport.ReporterTable, abusereport.ReporterColumn),
		)
		fromU = sqlgraph.SetNeighbors(arq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryReported chains the current query on the "reported" edge.
func (arq *AbuseReportQuery) QueryReported() *UserQuery {
	query := (&UserClient{config: arq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := arq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := arq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(abusereport.Table, abusereport.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, abusereport.ReportedTable, abusereport.ReportedColumn),
		)
		fromU = sqlgraph.SetNeighbors(arq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryShare chains the current query on the "share" edge.
func (arq *AbuseReportQuery) QueryShare() *ShareQuery {
	query := (&ShareClient{config: arq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := arq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := arq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(abusereport.Table, abusereport.FieldID, selector),
			sqlgraph.To(share.Table, share.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, abusereport.ShareTable, abusereport.ShareColumn),
		)
		fromU = sqlgraph.SetNeighbors(arq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first AbuseReport entity from the query.
// Returns a *NotFoundError when no AbuseReport was found.
func (arq *AbuseReportQuery) First(ctx context.Context) (*AbuseReport, error) {
	nodes, err := arq.Limit(1).All(setContextOp(ctx, arq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{abusereport.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (arq *AbuseReportQuery) FirstX(ctx context.Context) *AbuseReport {
	node, err := arq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first AbuseReport ID from the query.
// Returns a *NotFoundError when no AbuseReport ID was found.
func (arq *AbuseReportQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = arq.Limit(1).IDs(setContextOp(ctx, arq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{abusereport.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (arq *AbuseReportQuery) FirstIDX(ctx context.Context) int {
	id, err := arq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single AbuseReport entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one AbuseReport entity is found.
// Returns a *NotFoundError when no AbuseReport entities are found.
func (arq *AbuseReportQuery) Only(ctx context.Context) (*AbuseReport, error) {
	nodes, err := arq.Limit(2).All(setContextOp(ctx, arq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{abusereport.Label}
	default:
		return nil, &NotSingularError{abusereport.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (arq *AbuseReportQuery) OnlyX(ctx context.Context) *AbuseReport {
	node, err := arq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only AbuseReport ID in the query.
// Returns a *NotSingularError when more than one AbuseReport ID is found.
// Returns a *NotFoundError when no entities are found.
func (arq *AbuseReportQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = arq.Limit(2).IDs(setContextOp(ctx, arq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{abusereport.Label}
	default:
		err = &NotSingularError{abusereport.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (arq *AbuseReportQuery) OnlyIDX(ctx context.Context) int {
	id, err := arq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of AbuseReports.
func (arq *AbuseReportQuery) All(ctx context.Context) ([]*AbuseReport, error) {
	ctx = setContextOp(ctx, arq.ctx, "All")
	if err := arq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*AbuseReport, *AbuseReportQuery]()
	return withInterceptors[[]*AbuseReport](ctx, arq, qr, arq.inters)
}

// AllX is like All, but panics if an error occurs.
func (arq *AbuseReportQuery) AllX(ctx context.Context) []*AbuseReport {
	nodes, err := arq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of AbuseReport IDs.
func (arq *AbuseReportQuery) IDs(ctx context.Context) (ids []int, err error) {
	if arq.ctx.Unique == nil && arq.path != nil {
		arq.Unique(true)
	}
	ctx = setContextOp(ctx, arq.ctx, "IDs")
	if err = arq.Select(abusereport.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (arq *AbuseReportQuery) IDsX(ctx context.Context) []int {
	ids, err := arq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (arq *AbuseReportQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, arq.ctx, "Count")
	if err := arq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, arq, querierCount[*AbuseReportQuery](), arq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (arq *AbuseReportQuery) CountX(ctx context.Context) int {
	count, err := arq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (arq *AbuseReportQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, arq.ctx, "Exist")
	switch _, err := arq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (arq *AbuseReportQuery) ExistX(ctx context.Context) bool {
	exist, err := arq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AbuseReportQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (arq *AbuseReportQuery) Clone() *AbuseReportQuery {
	if arq == nil {
		return nil
	}
	return &AbuseReportQuery{
		config:       arq.config,
		ctx:          arq.ctx.Clone(),
		order:        append([]abusereport.OrderOption{}, arq.order...),
		inters:       append([]Interceptor{}, arq.inters...),
		predicates:   append([]predicate.AbuseReport{}, arq.predicates...),
		withReporter: arq.withReporter.Clone(),
		withReported: arq.withReported.Clone(),
		withShare:    arq.withShare.Clone(),
		// clone intermediate query.
		sql:  arq.sql.Clone(),
		path: arq.path,
	}
}

// WithReporter tells the query-builder to eager-load the nodes that are connected to
// the "reporter" edge. The optional arguments are used to configure the query builder of the edge.
func (arq *AbuseReportQuery) WithReporter(opts ...func(*UserQuery)) *AbuseReportQuery {
	query := (&UserClient{config: arq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	arq.withReporter = query
	return arq
}

// WithReported tells the query-builder to eager-load the nodes that are connected to
// the "reported" edge. The optional arguments are used to configure the query builder of the edge.
func (arq *AbuseReportQuery) WithReported(opts ...func(*UserQuery)) *AbuseReportQuery {
	query := (&UserClient{config: arq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	arq.withReported = query
	return arq
}

// WithShare tells the query-builder to eager-load the nodes that are connected to
// the "share" edge. The optional arguments are used to configure the query builder of the edge.
func (arq *AbuseReportQuery) WithShare(opts ...func(*ShareQuery)) *AbuseReportQuery {
	query := (&ShareClient{config: arq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	arq.withShare = query
	return arq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.AbuseReport.Query().
//		GroupBy(abusereport.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (arq *AbuseReportQuery) GroupBy(field string, fields ...string) *AbuseReportGroupBy {
	arq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &AbuseReportGroupBy{build: arq}
	grbuild.flds = &arq.ctx.Fields
	grbuild.label = abusereport.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.AbuseReport.Query().
//		Select(abusereport.FieldCreatedAt).
//		Scan(ctx, &v)
func (arq *AbuseReportQuery) Select(fields ...string) *AbuseReportSelect {
	arq.ctx.Fields = append(arq.ctx.Fields, fields...)
	sbuild := &AbuseReportSelect{AbuseReportQuery: arq}
	sbuild.label = abusereport.Label
	sbuild.flds, sbuild.scan = &arq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a AbuseReportSelect configured with the given aggregations.
func (arq *AbuseReportQuery) Aggregate(fns ...AggregateFunc) *AbuseReportSelect {
	return arq.Select().Aggregate(fns...)
}

func (arq *AbuseReportQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range arq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, arq); err != nil {
				return err
			}
		}
	}
	for _, f := range arq.ctx.Fields {
		if !abusereport.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if arq.path != nil {
		prev, err := arq.path(ctx)
		if err != nil {
			return err
		}
		arq.sql = prev
	}
	return nil
}

func (arq *AbuseReportQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*AbuseReport, error) {
	var (
		nodes       = []*AbuseReport{}
		_spec       = arq.querySpec()
		loadedTypes = [3]bool{
			arq.withReporter != nil,
			arq.withReported != nil,
			arq.withShare != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*AbuseReport).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &AbuseReport{config: arq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, arq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := arq.withReporter; query != nil {
		if err := arq.loadReporter(ctx, query, nodes, nil,
			func(n *AbuseReport, e *User) { n.Edges.Reporter = e }); err != nil {
			return nil, err
		}
	}
	if query := arq.withReported; query != nil {
		if err := arq.loadReported(ctx, query, nodes, nil,
			func(n *AbuseReport, e *User) { n.Edges.Reported = e }); err != nil {
			return nil, err
		}
	}
	if query := arq.withShare; query != nil {
		if err := arq.loadShare(ctx, query, nodes, nil,
			func(n *AbuseReport, e *Share) { n.Edges.Share = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (arq *AbuseReportQuery) loadReporter(ctx context.Context, query *UserQuery, nodes []*AbuseReport, init func(*AbuseReport), assign func(*AbuseReport, *User)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*AbuseReport)
	for i := range nodes {
		fk := nodes[i].ReporterUser
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "reporter_user" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (arq *AbuseReportQuery) loadReported(ctx context.Context, query *UserQuery, nodes []*AbuseReport, init func(*AbuseReport), assign func(*AbuseReport, *User)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*AbuseReport)
	for i := range nodes {
		fk := nodes[i].ReportedUser
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "reported_user" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (arq *AbuseReportQuery) loadShare(ctx context.Context, query *ShareQuery, nodes []*AbuseReport, init func(*AbuseReport), assign func(*AbuseReport, *Share)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*AbuseReport)
	for i := range nodes {
		fk := nodes[i].ShareReports
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(share.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "share_reports" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (arq *AbuseReportQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := arq.querySpec()
	_spec.Node.Columns = arq.ctx.Fields
	if len(arq.ctx.Fields) > 0 {
		_spec.Unique = arq.ctx.Unique != nil && *arq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, arq.driver, _spec)
}

func (arq *AbuseReportQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(abusereport.Table, abusereport.Columns, sqlgraph.NewFieldSpec(abusereport.FieldID, field.TypeInt))
	_spec.From = arq.sql
	if unique := arq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if arq.path != nil {
		_spec.Unique = true
	}
	if fields := arq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, abusereport.FieldID)
		for i := range fields {
			if fields[i] != abusereport.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if arq.withReporter != nil {
			_spec.Node.AddColumnOnce(abusereport.FieldReporterUser)
		}
		if arq.withReported != nil {
			_spec.Node.AddColumnOnce(abusereport.FieldReportedUser)
		}
		if arq.withShare != nil {
			_spec.Node.AddColumnOnce(abusereport.FieldShareReports)
		}
	}
	if ps := arq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := arq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := arq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := arq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (arq *AbuseReportQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(arq.driver.Dialect())
	t1 := builder.Table(abusereport.Table)
	columns := arq.ctx.Fields
	if len(columns) == 0 {
		columns = abusereport.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if arq.sql != nil {
		selector = arq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if arq.ctx.Unique != nil && *arq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range arq.predicates {
		p(selector)
	}
	for _, p := range arq.order {
		p(selector)
	}
	if offset := arq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := arq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// AbuseReportGroupBy is the group-by builder for AbuseReport entities.
type AbuseReportGroupBy struct {
	selector
	build *AbuseReportQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (argb *AbuseReportGroupBy) Aggregate(fns ...AggregateFunc) *AbuseReportGroupBy {
	argb.fns = append(argb.fns, fns...)
	return argb
}

// Scan applies the selector query and scans the result into the given value.
func (argb *AbuseReportGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, argb.build.ctx, "GroupBy")
	if err := argb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AbuseReportQuery, *AbuseReportGroupBy](ctx, argb.build, argb, argb.build.inters, v)
}

func (argb *AbuseReportGroupBy) sqlScan(ctx context.Context, root *AbuseReportQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(argb.fns))
	for _, fn := range argb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*argb.flds)+len(argb.fns))
		for _, f := range *argb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*argb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := argb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// AbuseReportSelect is the builder for selecting fields of AbuseReport entities.
type AbuseReportSelect struct {
	*AbuseReportQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ars *AbuseReportSelect) Aggregate(fns ...AggregateFunc) *AbuseReportSelect {
	ars.fns = append(ars.fns, fns...)
	return ars
}

// Scan applies the selector query and scans the result into the given value.
func (ars *AbuseReportSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ars.ctx, "Select")
	if err := ars.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AbuseReportQuery, *AbuseReportSelect](ctx, ars.AbuseReportQuery, ars, ars.inters, v)
}

func (ars *AbuseReportSelect) sqlScan(ctx context.Context, root *AbuseReportQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ars.fns))
	for _, fn := range ars.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ars.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ars.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
